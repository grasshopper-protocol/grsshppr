# Next

Agreed direction, not yet started. Major items need an [RFC](../rfc) before
they move to **Now**.

- **Session reschedule flow** — cancel shipped in 0.1.0; reschedule is the
  remaining gap (move a session without losing the thread). See
  [RFC-0007](../rfc/RFC-0007-session-reschedule.md) (Draft). _Why:_ real
  mentoring needs to handle life happening.
- **Mentor visibility & ranking** — gate `/explore` on profile completeness and
  rank by signals we already collect. _Why:_ oldest-first sorting rewards
  incumbency and surfaces half-built profiles. See
  [RFC-0001](../rfc/RFC-0001-mentor-visibility-ranking.md).

> Items here are committed in principle. Promotion to **Now** requires an owner,
> an open issue, and (for major changes) an approved RFC.
